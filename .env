# jobSearchRust Environment Configuration
# Generated by JHipster

# Application
APP_NAME=jobSearchRust
APP_ENV=development
APP_PORT=8080
APP_HOST=0.0.0.0
# Set to true when using HTTPS (e.g., in production with TLS)
# APP_HTTPS=false

# Database
DATABASE_URL=postgres://jobSearchRust@localhost:5432/jobSearchRust

# JWT Configuration
# JWT_SECRET is a random 256-bit value generated at scaffold time.
# Override it (e.g. with `openssl rand -hex 32`) before deploying to production.
JWT_SECRET=094a36575ef815578101ae913d6952b393920ea1bbe21e6ba4a39e8561a71a2a
JWT_EXPIRATION_HOURS=24

# Static Files Configuration
# Set SERVE_STATIC_FILES=true to serve the SPA UI from the Rust backend
# STATIC_FILES_DIR specifies the directory containing the built Angular/React/Vue app
# SERVE_STATIC_FILES=true
# STATIC_FILES_DIR=./static








# Logging
RUST_LOG=info,jobSearchRust=debug,tower_http=debug
